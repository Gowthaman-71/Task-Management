package com.example.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class FirebaseConfig {
    // Firebase Admin SDK configuration removed in favor of REST API usage in FirebaseService.
    // This class is kept for potential future use or to avoid breaking other dependencies.
}
